{
  "name": "tech-site-builder",
  "version": "1.0.0",
  "description": "Builder for Jean Meira's tech content site",
  "main": "build.js",
  "scripts": {
    "build": "NODE_ENV=production node build.js",
    "build-local": "NODE_ENV=development node build.js",
    "dev": "NODE_ENV=development node build.js && python3 -m http.server 8001 --directory ../dist",
    "clean": "rm -rf dist/*",
    "test": "node build.js"
  },
  "dependencies": {
    "marked": "^4.3.0",
    "js-yaml": "^4.1.0",
    "mustache": "^4.2.0",
    "fs-extra": "^11.1.1"
  },
  "devDependencies": {
    "clean-css": "^5.3.2",
    "terser": "^5.24.0"
  },
  "author": "Jean Meira",
  "license": "MIT"
}
